using System;
using System.Linq;
using System.Threading.Tasks;
using Microsoft.AspNetCore.Authorization;

namespace AGSIdentity.Services.ProfileService.Identity
{
    //public class AGSAuthorizationHandler : IAuthorizationHandler
    //{
    //    public Task HandleAsync(AuthorizationHandlerContext context)
    //    {
    //        var pendingRequirements = context.PendingRequirements.ToList();

    //        foreach (var requirement in pendingRequirements)
    //        {
    //            ReadPermission
    //        }

    //        //Always allow admin to execute
    //        if (context.User.Identity.Name == AGSCommon.CommonConstant.AGSIdentityConstant.AGSAdminName)
    //        {
                
    //        }

    //        return Task.CompletedTask;
    //    }
    //}
}
